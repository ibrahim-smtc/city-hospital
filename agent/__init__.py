"""
Objective: Expose the compiled agent graph so it can be easily imported into other parts of the application.

Tasks:
1. Import the compiled graph from `agent.graph`.
2. Ensure that importing `from agent import graph` works seamlessly in `routers/chat.py`.
"""
